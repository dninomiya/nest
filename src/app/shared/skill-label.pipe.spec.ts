import { SkillLabelPipe } from './skill-label.pipe';

describe('SkillLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
